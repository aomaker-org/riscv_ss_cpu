# TODO for rpi_docker50701

-   **Review and complete `doit.md`:**
    The current content of `doit.md` appears to be incomplete. It was likely interrupted during its creation or pasting. The file ends abruptly after the initial "Usage:" code block.
    The more complete version of `doit.md` (describing workflow commands like `cleanrestart` and individual commands like `install`, `build`, `run`, etc.) was previously provided as part of the content for `gemini.md`. This more complete version should be reviewed and used to finalize `doit.md`.
